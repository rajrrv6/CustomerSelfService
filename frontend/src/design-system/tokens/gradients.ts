/**
 * Design System — Enterprise Gradient Tokens
 * Curated gradient backgrounds for hero sections, banners, and status indicators.
 */

// ─────────────────────────────────────────────────────────────────────────────
// Primary Brand Gradients
// ─────────────────────────────────────────────────────────────────────────────

/** Primary hero gradient — portal home, AI workspace banner */
export const GRADIENT_PRIMARY = 'bg-linear-to-r from-blue-600 to-indigo-700';

/** Softer blue-to-violet — AI suggestions, feature callouts */
export const GRADIENT_ACCENT = 'bg-linear-to-br from-blue-500/10 to-violet-500/10';

/** Dark gradient — sidebar panel backgrounds */
export const GRADIENT_DARK = 'bg-linear-to-b from-slate-900 to-slate-950';

// ─────────────────────────────────────────────────────────────────────────────
// Status Gradients
// ─────────────────────────────────────────────────────────────────────────────

/** Success gradient — resolved ticket, CSAT confirmation */
export const GRADIENT_SUCCESS = 'bg-linear-to-r from-emerald-500 to-green-600';

/** Warning gradient — near SLA breach, queue alerts */
export const GRADIENT_WARNING = 'bg-linear-to-r from-amber-400 to-orange-500';

/** Danger gradient — SLA breached, system offline */
export const GRADIENT_DANGER = 'bg-linear-to-r from-rose-500 to-red-600';

// ─────────────────────────────────────────────────────────────────────────────
// Subtle Background Gradients
// ─────────────────────────────────────────────────────────────────────────────

/** Light slate wash — alternating row backgrounds */
export const GRADIENT_WASH = 'bg-linear-to-b from-slate-50 to-white dark:from-slate-950 dark:to-slate-900';
