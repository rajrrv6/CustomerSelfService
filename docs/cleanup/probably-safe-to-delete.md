# Probably Safe to Delete Analysis (Low-Risk Candidates)
## CustomerSelfService Platform — Repo Hygiene

**Audit Date:** 2026-06-03  
**Last Updated:** 2026-06-03T16:06:48+05:30  
**Auditor:** Senior Repository Cleanup Auditor (Antigravity)  

---

## 1. Overview & Summary

This report lists files and utility scripts that are **not directly imported** in the active application runtime code or routing logic. However, they might represent developer utility scripts, barrel exports, or temporary compilation targets. Deleting them is considered **low-risk** but requires verification of developer workflows (e.g. if the localization script is still needed for future updates).

---

## 2. Inventory of Probably Safe-to-Delete Files

### A. Barrel Exports & Index Files
These index files are not imported by the main Next.js routes because components prefer direct, explicit imports. They can be safely deleted, though keeping them does not bloat bundle size (only affects source organization).

| File Path | Description / Purpose | Risk Assessment | Recommendation |
|---|---|---|---|
| `frontend/src/stores/index.ts` | Barrel index file exporting `useUIStore`, `useAuthStore`, `useNotificationsStore`. | Components import directly from specific store files (e.g. `@/stores/uiStore`). No active imports found. | **Delete** to enforce direct, consistent imports across components. |

---

### B. Temporary Translation Artifacts
This typescript file contains translation mappings generated by a subagent. It is located at the root of the `frontend` directory and is not part of the active source directory.

| File Path | Description / Purpose | Risk Assessment | Recommendation |
|---|---|---|---|
| `frontend/translations_complete_from_subagent.ts` | 28.3 KB translation file generated during the June 1/2 audit phases. | Contains Arabic translation keys. Verify whether all translation keys inside this file have been successfully merged into `frontend/src/i18n/ar.ts` before pruning. | **Merge & Delete.** Once verified that `ar.ts` matches (already confirmed at 1,043 keys), delete this file. |

---

### C. Translation Utility Scripts
These python scripts are used to extract, copy, and merge Arabic translation keys. They are in the root directory and are not imported.

| File Path | File Size | Purpose | Risk Assessment | Recommendation |
|---|---|---|---|---|
| `frontend/copy_translations.py` | 351 B | Developer script for translations | Only needed if updating i18n dictionaries | **Move to `scripts/`** folder if needed for future translation runs; otherwise delete. |
| `frontend/extract_ar.py` | 339 B | Key extractor for Arabic translations | Only needed if updating i18n dictionaries | **Move to `scripts/`** or delete. |
| `frontend/extract_ar_portal.py` | 607 B | Key extractor for portal translations | Only needed if updating i18n dictionaries | **Move to `scripts/`** or delete. |
| `frontend/extract_ar_prompt.py` | 1.5 KB | Prompt builder for translation agent | Only needed if translating with AI agents | **Move to `scripts/`** or delete. |
| `frontend/merge_ar.py` | 25.5 KB | Complex merging tool for translation keys | Only needed if resolving translation conflicts | **Move to `scripts/`** or delete. |

---

## 3. Recommended Actions

1. **Verify i18n Merges:** Run a brief check to confirm all strings in `translations_complete_from_subagent.ts` exist in `src/i18n/ar.ts`.
2. **Move Developer Utilities:** Create a new `/scripts/` folder at the root level (`/Users/sudhir88/Desktop/CustomerSelfService/scripts/`) and relocate `copy_translations.py`, `extract_ar*.py`, and `merge_ar.py` there to clean the root index.
3. **Prune Index Barrel:** Remove `src/stores/index.ts`.
